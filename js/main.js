// Smooth Scroll
		    $(document).ready(function() {
		    
		      $('.nav a').smoothScroll({offset: -73});
		    
		    });

// Scrollspy offset
$('[data-spy="scroll"]').scrollspy({ offset: 100 });